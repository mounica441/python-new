from setuptools import find_packages, setup

setup(
    name="medical_chatbot",
    version="0.1.0",
    author="************(user name git hub)",
    author_email="***********@gmail.com(email git hub)",
    packages=find_packages(),
    install_requires=[]
)
